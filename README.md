# Homebrew Tap for TrustCheck

Install TrustCheck with Homebrew:

```sh
brew tap Halfblood-Prince/tap
brew install trustcheck
```

Or install directly:

```sh
brew install Halfblood-Prince/tap/trustcheck
```