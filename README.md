# Homebrew Tap

Install packages with Homebrew:

```sh
brew tap Halfblood-Prince/tap
brew install trustcheck
brew install pi-doctor
```

Or install directly:

```sh
brew install Halfblood-Prince/tap/trustcheck
brew install Halfblood-Prince/tap/pi-doctor
```

`pi-doctor` is published here for Linux Homebrew installations.
